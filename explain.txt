================================================================================
   ZINDI FINANCIAL STRESS PREDICTION CHALLENGE - MASTER LEARNING & VIVA GUIDE (V6)
================================================================================

PROJECT: Mobile-Money Liquidity Stress Prediction Challenge
PLATFORM: Zindi Competition Track
TEAM: Sharruk S, Shalini M, Nathaniel Christian
GOAL: Predict the probability of a customer experiencing liquidity/financial stress
      within the next 30 days based on their profile and 6-month transaction history.
TARGET BENCHMARK: Rank 1 on Zindi Multi Score (> 0.7394)

================================================================================
TABLE OF CONTENTS (CHRONOLOGICAL STEP-BY-STEP LEARNING JOURNEY)
================================================================================
STEP 1: Understanding the Business & ML Problem Formulation
STEP 2: Dataset Anatomy & Feature Exploration (EDA)
STEP 3: The 85/15 Imbalance Problem & Why Accuracy Fails
STEP 4: Designing a Zero-Leakage 10-Fold Stratified Validation Harness
STEP 5: Grand Master v6 Feature Engineering Engine (488 Total Columns)
STEP 6: 8-Model Orthogonal Suite with CatBoost & NVIDIA RTX GPU Acceleration
STEP 7: Dual-Metric SLSQP Blending, Temperature Calibration ($T$) & Prior Alignment
STEP 8: Leaderboard Metrics & Mathematical Gap Analysis
STEP 9: Execution Guide (How to Run Full 40,000-Sample Training on GPU)
STEP 10: Master Viva / Professor Defense Q&A

================================================================================
STEP 1: UNDERSTANDING THE BUSINESS & ML PROBLEM FORMULATION
================================================================================
1. What is Liquidity Stress?
   - A customer is in "liquidity stress" when they lack sufficient immediately
     available cash or mobile-money balance to cover upcoming routine obligations
     (e.g., utility paybills, food/merchant purchases, P2P transfers).
   - A person may have historical wealth, but if cash inflows freeze and balances
     drop below obligations, they suffer liquidity failure.

2. The Machine Learning Framing:
   - Observation Window: Trailing 6 months of customer transactions (M1 to M6).
     * M1 = most recent month before observation date.
     * M6 = oldest month (6 months ago).
   - Target Horizon: 30 days forward into the future from observation date.
   - Task: Binary Probability Classification.
     * 0 = No liquidity stress in next 30 days.
     * 1 = Liquidity stress occurs in next 30 days.
   - Output: Continuous risk probability [0.0 to 1.0].

================================================================================
STEP 2: DATASET ANATOMY & FEATURE EXPLORATION (EDA)
================================================================================
- Training Set (Train.csv): 40,000 rows x 184 columns.
- Test Set (Test.csv): 30,000 rows x 183 columns (unlabeled).
- Submission Target: Predict calibrated probability for all 30,000 test IDs.

- Breakdown of the 184 Raw Columns:
  1. Identifier (1 col): `ID` (Hexadecimal ID -> dropped from model input).
  2. Customer Profile (7 cols):
     - `arpu`: Average Revenue Per User.
     - `age`: Customer age in years (18 to 70).
     - `gender`: M / F.
     - `region`: Geographic region (7 categories).
     - `smartphone`: Yes / No.
     - `segment`: Value tier (Low/Medium/High Value Customer: LVC, MVC, HVC).
     - `earning_pattern`: Monthly / Weekly / Irregular Earner.
  3. Mobile-Money Activity Rate (1 col):
     - `x_90_d_activity_rate`: Proportion of active mobile-money days in last 90 days.
  4. Daily Average Balances (6 cols):
     - `m1_daily_avg_bal` to `m6_daily_avg_bal`.
  5. Behavioral Transactions (168 cols):
     - 7 Transaction Channels:
       a. `paybill`: Utility / bill payments.
       b. `merchantpay`: Merchant retail purchases.
       c. `transfer_from_bank`: Inflows from linked bank account.
       d. `mm_send`: Peer-to-peer mobile money sent.
       e. `received`: Peer-to-peer mobile money received from friends/family.
       f. `deposit`: Cash deposited via physical mobile-money agents.
       g. `withdraw`: Cash withdrawn via physical agents.
     - 4 Metrics Per Month per Channel:
       * `_volume`: Count of transactions that month.
       * `_total_value`: Sum of transacted money.
       * `_highest_amount`: Largest single transaction that month.
       * `_counterparty_count`: Unique entities interacted with (companies, merchants, banks, recipients, senders, agents).
       * (7 channels x 6 months x 4 metrics = 168 behavioural columns).
  6. Target Label (1 col): `liquidity_stress_next_30d` (Train only).

================================================================================
STEP 3: THE 85/15 IMBALANCE PROBLEM & WHY ACCURACY FAILS
================================================================================
1. The Observed Target Distribution:
   - Class 0 (No Stress): 34,000 customers (85.0%)
   - Class 1 (Stress):     6,000 customers (15.0%)

2. Why Accuracy is a Misleading Metric:
   - If a naive model predicts "0" for 100% of customers, it achieves 85% Accuracy!
   - However, it detects exactly 0% of stressed customers, making it useless for business.

3. The Proper Evaluation Formulation:
   - Evaluated using Log Loss (cross-entropy certainty) and ROC-AUC (ranking power).
   - Unweighted posterior probability outputs ensure that predicted probabilities match
     the true 15% class base rate ($E[P(Y=1)] \approx 0.15$), optimizing the combined Multi Score.

================================================================================
STEP 4: DESIGNING A ZERO-LEAKAGE 10-FOLD STRATIFIED VALIDATION HARNESS
================================================================================
1. Why 10-Fold Stratified Cross-Validation?
   - 10 folds provide 36,000 training rows per fold (90% of dataset), maximizing data utilization.
   - Ensembling 10 models per architecture reduces variance by $\sqrt{10} \approx 3.16\times$.
   - Preserves the exact 85/15 class ratio across all 10 folds.

2. Preventing Data Leakage (Strict Fold Isolation):
   - Target Encoding and feature scalers are fit strictly on the 90% training fold slice.
   - Validation slices and test data only use pre-fitted mappings.
   - Zero global target statistics leak across validation boundaries.

================================================================================
STEP 5: GRAND MASTER V6 FEATURE ENGINEERING ENGINE (488 TOTAL COLUMNS)
================================================================================
Raw monthly numbers are static. To detect stress, we engineered dynamic temporal patterns:

A. Financial Physics & Exponential Moving Averages (EMA):
   - Half-life weighted averages ($\alpha=0.5, \alpha=0.25$) emphasizing recent months ($M1, M2$).
   - Cumulative area under the balance curve ($\int \text{balance}(t) dt$).

B. Second-Order Acceleration & Jerk:
   - Discrete second derivatives: $(M1 - 2M2 + M3), (M2 - 2M3 + M4)$ capturing whether balance decline is speeding up.

C. Liquidity Exhaustion Days Countdown:
   - Mathematical countdown of days until balance reaches 0 based on net burn rate.

D. Month-over-Month Velocity & Differences:
   - $M1-M2, M2-M3, M3-M4, M4-M5, M5-M6$ across values, volumes, balances, and counterparties.

E. Rolling Baseline Comparison:
   - `bal_recent_vs_old_ratio`: `mean(M1..M3) / (mean(M4..M6) + 1)` (consistently ranks #1 in tree feature importance!).

F. Spending Gini Inequality:
   - Gini coefficient measuring inequality in transaction sizes across 6 months.

G. Unsupervised Financial Personas (K-Means Clustering):
   - 8 K-Means cluster distance features capturing distinct behavioral archetypes.

H. Composite Heuristic Stress Index:
   - Weighted composite index combining negative slope, high deficit months, low coverage,
     inflow collapse, and liquidity exhaustion flags.

================================================================================
STEP 6: 8-MODEL ORTHOGONAL SUITE WITH CATBOOST & NVIDIA RTX GPU ACCELERATION
================================================================================
1. LightGBM Deep GBDT:
   - 1800 trees, learning_rate=0.015, num_leaves=63, max_depth=8, colsample_bytree=0.6.

2. LightGBM DART Booster:
   - 1400 trees, learning_rate=0.022, drop_rate=0.1, skip_drop=0.5.

3. LightGBM GOSS Booster:
   - Gradient-based one-side sampling for difficult boundary samples.

4. XGBoost (GPU Accelerated on RTX 3050):
   - `device='cuda'`, `tree_method='hist'`, 1500 trees, learning_rate=0.015, reg_lambda=5.0.

5. CatBoost (GPU Accelerated on RTX 3050):
   - Oblivious symmetric decision trees (takes 40% ensemble weight!).

6. HistGradientBoosting (Scikit-Learn):
   - Fast histogram binner with native missing value support.

7. ExtraTrees (Variance Reduction):
   - 600 randomized decision trees voting in parallel.

8. PyTorch Deep Tabular ResMLP:
   - Multi-layer Perceptron with BatchNorm, Mish activations, Dropout, and Residual Skip Connections.

================================================================================
STEP 7: DUAL-METRIC SLSQP BLENDING, TEMPERATURE CALIBRATION & PRIOR ALIGNMENT
================================================================================
1. Temperature Scaling ($T$ Calibration):
   - Post-processing probability calibration:
     $$P_{\text{calibrated}} = \sigma\left(\frac{\text{logit}(P)}{T}\right)$$
   - Softens overconfident probabilities without altering ROC-AUC rank order.

2. Dual-Metric SLSQP Optimal Blending:
   - Non-negative weights $(w_1, ..., w_k)$ summing to 1.0 optimizing:
     $$\min_w \text{LogLoss}(y_{\text{true}}, \sum w_i P_i) - 0.12 \times \text{ROC\_AUC}$$

3. Empirical Base-Rate Prior Alignment:
   - Uses Bayesian Odds Ratio alignment to guarantee that the test distribution perfectly
     matches the true 15.0% training base rate ($E[P(Y=1)] = 0.1500$).

================================================================================
STEP 8: LEADERBOARD METRICS & MATHEMATICAL GAP ANALYSIS
================================================================================
- Rank 79: Multi Score = 0.7040 | ROC-AUC = 0.9016 | Log Loss = 0.2545
- Rank 68: Multi Score = 0.7135 | ROC-AUC = 0.9085 | Log Loss = 0.2477
- Rank 1:  Multi Score = 0.7394 | ROC-AUC = 0.9224 | Log Loss = 0.2277
- With 488 features, CatBoost GPU, and Prior Alignment, our models target the Top 10!

================================================================================
STEP 9: EXECUTION GUIDE (HOW TO RUN FULL TRAINING ON GPU)
================================================================================
1. Activate Environment (Windows Git Bash):
   $ source venv/Scripts/activate

2. Run Full 10-Fold Model Training on all 40,000 samples (auto uses NVIDIA GPU):
   $ python train.py

3. Output Location:
   - Primary Submission CSV: `model/data/submissions/submission.csv`
   - Upload `submission.csv` directly to the Zindi competition portal.

================================================================================
STEP 10: MASTER VIVA / PROFESSOR DEFENSE Q&A
================================================================================
Q1: What is the core business objective and why is binary classification appropriate?
A: The objective is early intervention for customers at risk of mobile-money liquidity
   stress over a 30-day forward window. Since stress is an either/or event within a
   fixed horizon, binary classification with calibrated probability output is the ideal formulation.

Q2: Why not rely on standard accuracy for evaluation?
A: The dataset has an 85% negative / 15% positive class imbalance. A trivial dummy
   model predicting zero achieves 85% accuracy but is commercially useless. We evaluate
   using Log Loss and ROC-AUC with Stratified 10-Fold Cross-Validation.

Q3: How do you guarantee that no data leakage occurred in your pipeline?
A: Preprocessing steps, such as categorical target encoding, scaling, and cluster mappings,
   are computed strictly inside the training portion of each fold. Validation and test folds
   are only transformed using pre-fitted fold statistics.

Q4: What role does CatBoost play in your ensemble?
A: CatBoost builds symmetric oblivious decision trees, which choose the exact same splitting
   criterion across an entire tree level. This creates a completely orthogonal decision boundary
   compared to depth-wise (XGBoost) and leaf-wise (LightGBM) algorithms, earning it the highest
   weight in our SLSQP ensemble.

Q5: What is Prior Probability Alignment?
A: Prior Probability Alignment adjusts the intercept in log-odds space so that the average
   predicted test probability matches the empirical population prior (15.0%). This eliminates
   systematic probability bias and directly minimizes Log Loss penalty on test evaluation.
================================================================================
